#include <igl/opengl/glfw/Viewer.h>
#include <iostream>

int main(int argc, char *argv[]) {
    // TODO: Load mesh, set up viewer, implement ARAP deformation
    std::cout << "ARAP Deformer - placeholder" << std::endl;

    igl::opengl::glfw::Viewer viewer;
    viewer.launch();

    return 0;
}
